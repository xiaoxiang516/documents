```vue
<el-input placeholder="点击选择" v-model="formObj.formData.FqnumTxt" readonly>
  <template #suffix>
    <el-icon>
      <search />
    </el-icon>
  </template>
</el-input>
```

```typescript
  // 日期格式转换
  const formatDate = (date) => {
    return (!date || date === '00000000') ? '' : moment(date, 'YYYYMMDD').format('YYYY-MM-DD')
  }

  const formatDateToServer = (date) => {
    return date ? moment(date, 'YYYY-MM-DD').format('YYYYMMDD') : ''
  }
```
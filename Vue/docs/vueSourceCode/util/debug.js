import config from "../"
import { noop } from 'shared/util'
export let warn = noop;
export let tip = noop;
export let generateComponentTrace = (noop:any)
export let formatComponetName = (noop: any)

if(process.env)